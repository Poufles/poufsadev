const root = document.querySelector(`:root`);
const canvas = document.querySelector(`.canvas`);
const input_pixels = document.querySelector(`.pixels`);
const btn_rainbow = document.querySelector(`.rainbow-container`);
const initialColors = document.querySelector(`.initial-colors`)
const btn_initialColors = initialColors.querySelectorAll(`button`);
const btn_erase = document.querySelector(`.erase`);
const btn_clear = document.querySelector(`.clear`);
const input_hexCode = document.querySelector(`.player-color`);
const customColorPreview = document.querySelector(`.custom-color-preview`);